export const timing = {
  /**
   * The duration (ms) for quick animations.
   */
  quick: 300,
  /**
   * The duration (ms) for long animations.
   */
  long: 500,
  /**
   * The duration (ms) for very long animations.
   */
  veryLong: 1000,
}
